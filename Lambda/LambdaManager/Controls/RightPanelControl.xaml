<UserControl x:Class="LambdaManager.Controls.RightPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:im="clr-namespace:ColorVision.ImageEditor;assembly=ColorVision.ImageEditor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="RadiusBorder" TargetType="{x:Type Border}">
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="Border.CornerRadius" Value="5" />
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        
        <!-- 图像显示区域 -->
        <Border Name="imagingView" 
                Grid.Row="0"
                Style="{StaticResource RadiusBorder}" 
                Background="{DynamicResource WindowBackgroundBrush}" 
                Padding="3">
            <im:ImageView/>
        </Border>
        
        <!-- 消息列表显示区域 -->
        <ComboBox Name="msgList" 
                  Grid.Row="1" 
                  SelectedIndex="0" 
                  BorderThickness="0" 
                  VerticalAlignment="Center" 
                  HorizontalContentAlignment="Left" 
                  HorizontalAlignment="Right" 
                  Width="400" 
                  Height="30"
                  ItemsSource="{Binding Messages}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Path=Text}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</UserControl>