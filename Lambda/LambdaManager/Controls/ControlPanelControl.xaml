<UserControl x:Class="LambdaManager.Controls.ControlPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    
    <DockPanel Margin="5">
        <!-- 左侧预览按钮 -->
        <ToggleButton Width="95" 
                      Height="25" 
                      DockPanel.Dock="Left"  
                      Style="{DynamicResource IndicateButton}" 
                      Content="预览"
                      Command="{Binding PreviewCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      CommandParameter="{Binding IsChecked, RelativeSource={RelativeSource Self}}"/>
        
        <!-- 右侧控制按钮组 -->
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
            <ToggleButton Width="95"  
                          Style="{DynamicResource IndicateButton}" 
                          Content="开始采集"
                          Command="{Binding StartAcquireCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          CommandParameter="{Binding IsChecked, RelativeSource={RelativeSource Self}}"/>
            <Button Width="70" 
                    Height="25" 
                    Content="拍照"
                    Command="{Binding SnapshotCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </StackPanel>
        
        <!-- 中间空白区域 -->
        <TextBlock />
    </DockPanel>
</UserControl>