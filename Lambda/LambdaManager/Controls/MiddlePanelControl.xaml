<UserControl x:Class="LambdaManager.Controls.MiddlePanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200">
    <UserControl.Resources>
        <Style x:Key="RadiusBorder" TargetType="{x:Type Border}">
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="Border.CornerRadius" Value="5" />
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        
        <!-- 材质视图区域 -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Border Style="{StaticResource RadiusBorder}">
                <StackPanel Name="materialView" Margin="2,2,4,2">
                    <TextBlock>Workspace 1</TextBlock>
                </StackPanel>
            </Border>
        </ScrollViewer>
        
        <!-- 控制按钮区域 -->
        <StackPanel Grid.Row="1" Margin="5" Orientation="Horizontal">
            <Button Width="60" 
                    Margin="10,0,10,0" 
                    Content="多模态"
                    Command="{Binding MultiModeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <Button Width="60" 
                    Margin="0,0,10,0" 
                    Content="多通道"
                    Command="{Binding MultiChannelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </StackPanel>
    </Grid>
</UserControl>